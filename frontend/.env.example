# Vercel only (Project -> Settings -> Environment Variables) — NOT used by Vite in the browser.
# Public HTTPS URL that reaches your local backend (run start-tunnel.bat, copy the https link).
# Example: https://something.loca.lt
# No trailing slash. Redeploy after changing.

BACKEND_URL=
